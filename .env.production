# ╔══════════════════════════════════════════════════════════════════╗
# ║  Codex Platform — Production Environment Variables              ║
# ║  Copy this file to .env.production on your EC2 instance         ║
# ║  and fill in the real values.                                   ║
# ╚══════════════════════════════════════════════════════════════════╝

# ── Neon PostgreSQL (Cloud DB) ────────────────────────────────────
# Get these from your Neon dashboard → Connection Details
# docker-compose expects NEON_DB_* and maps them to DB_* in the container
NEON_DB_HOST=ep-hidden-king-a10oexyf-pooler.ap-southeast-1.aws.neon.tech
NEON_DB_PORT=5432
NEON_DB_NAME=codex-db
NEON_DB_USERNAME=neondb_owner
NEON_DB_PASSWORD=npg_tXkVJ5Dy4lhe

# ── Hosted Redis ──────────────────────────────────────────────────
# Full Redisson URL: redis:// (plain) or rediss:// (TLS)
REDIS_URL=redis://redis-11661.c212.ap-south-1-1.ec2.cloud.redislabs.com:11661
REDIS_PASSWORD=5SNfwcUb5vA7VloU9I2aSUhrSm0eGzI


# ── JWT Secret ────────────────────────────────────────────────────
# MUST be at least 32 characters. Generate with:
#   openssl rand -base64 48
JWT_SECRET=this-default-secret-is-for-local-dev-only-change-me-1234567890
JWT_EXPIRATION=86400000

# App port (matches docker-compose port mapping)
PORT=8080

# ── CORS ──────────────────────────────────────────────────────────
# Comma-separated frontend origins. Use * for open access.
APP_CORS_ALLOWED_ORIGINS=https://localhost:3000

# Logging
LOG_LEVEL=INFO
